# تحديث صفحة التقارير - عرض إحصائيات العملات المنفصلة

## 📋 نظرة عامة

تم تحديث صفحة التقارير لعرض **إحصائيات منفصلة ودقيقة** للمدفوعات والمبيعات بكل عملة (USD و IQD) بشكل واضح واحترافي.

---

## ✅ التحديثات المنفذة

### 1. الباك إند (Backend)

#### تحديث `saleService.js` - دالة `getSalesReport()`

**الإضافات:**

```javascript
// إحصائيات منفصلة لكل عملة
currencyStats: {
  USD: {
    totalSales: 0,      // إجمالي المبيعات بالدولار
    totalPaid: 0,       // إجمالي المدفوع بالدولار
    totalRemaining: 0,  // المتبقي بالدولار
    count: 0            // عدد المبيعات بالدولار
  },
  IQD: {
    totalSales: 0,      // إجمالي المبيعات بالدينار
    totalPaid: 0,       // إجمالي المدفوع بالدينار
    totalRemaining: 0,  // المتبقي بالدينار
    count: 0            // عدد المبيعات بالدينار
  }
},
// للتوافق المباشر
salesUSD: 0,          // إجمالي المبيعات بالدولار
paidUSD: 0,           // إجمالي المدفوع بالدولار
salesIQD: 0,          // إجمالي المبيعات بالدينار
paidIQD: 0            // إجمالي المدفوع بالدينار
```

**الفوائد:**

- ✅ بيانات دقيقة لكل عملة
- ✅ لا حاجة لحسابات في الفرونت إند
- ✅ جاهزة للعرض مباشرة

---

### 2. الفرونت إند (Frontend)

#### أ. قسم "الإحصائيات حسب العملة" (جديد)

**الموقع:** بعد البطاقات الرئيسية مباشرة

**المميزات:**

- 🎨 تصميم جذاب بألوان متناسقة
- 💵 قسم منفصل للدولار (USD) بلون أزرق
- 💰 قسم منفصل للدينار (IQD) بلون ذهبي
- 📊 عرض واضح للمبيعات والمدفوع والمتبقي

**المحتوى لكل عملة:**

1. **إجمالي المبيعات** - بطاقة خضراء
2. **إجمالي المدفوع** - بطاقة زرقاء
3. **المتبقي** - بطاقة برتقالية (محسوب تلقائياً)

---

#### ب. دوال التنسيق الجديدة

##### `formatUSD(amount)`

```javascript
// تنسيق الدولار مع $
// مثال: formatUSD(1234.56) → "$ 1,234.56"
```

##### `formatIQD(amount)`

```javascript
// تنسيق الدينار مع IQD
// مثال: formatIQD(1500000) → "1,500,000 IQD"
```

**الميزات:**

- ✅ تنسيق رقمي صحيح (فواصل للآلاف)
- ✅ رمز العملة واضح
- ✅ دقة مناسبة (رقمين عشريين للدولار، لا عشريات للدينار)

---

#### ج. تحديث التصدير (Excel & PDF)

##### Excel (CSV)

**الإضافة:**

```csv
إحصائيات العملات
العملة,إجمالي المبيعات,إجمالي المدفوع,المتبقي
دولار (USD),1000.00,600.00,400.00
دينار (IQD),1500000,900000,600000
```

##### PDF

**الإضافة:**

- جدول منفصل لـ "إحصائيات العملات"
- عرض منسق لكل عملة مع أيقوناتها
- تنسيق احترافي للطباعة

---

## 🎯 الاعتبارات التقنية المطبقة

### 1. ✅ تحديث ديناميكي للبيانات

- البيانات تُحدّث تلقائياً مع كل استعلام
- لا حاجة لإعادة تحميل الصفحة

### 2. ✅ تنسيق دقيق للأرقام والعملات

- استخدام `toLocaleString()` للتنسيق
- دقة مناسبة لكل عملة
- فواصل للآلاف لسهولة القراءة

### 3. ✅ تحسين واجهة المستخدم

- تصميم بطاقات واضح ومنظم
- ألوان متناسقة ومميزة لكل عملة
- أيقونات واضحة (💵 للدولار، 💰 للدينار)
- مسافات ملائمة بين العناصر

### 4. ✅ الأداء

- حسابات في الباك إند فقط
- الفرونت إند يعرض البيانات مباشرة
- لا توجد حسابات معقدة في المتصفح

### 5. ✅ تجربة المستخدم

- سهولة القراءة والفهم
- معلومات واضحة ومباشرة
- تنظيم منطقي للبيانات

---

## 📊 مثال على البيانات المعروضة

### سيناريو:

- مبيعات USD: $5,000
- مدفوع USD: $3,200
- مبيعات IQD: 7,500,000 IQD
- مدفوع IQD: 4,500,000 IQD

### العرض في الواجهة:

**قسم دولار أمريكي (USD):**

```
┌─────────────────────────────────┐
│ 💵 دولار أمريكي (USD)          │
├─────────────────────────────────┤
│ إجمالي المبيعات                 │
│     $ 5,000.00                  │
├─────────────────────────────────┤
│ إجمالي المدفوع                  │
│     $ 3,200.00                  │
├─────────────────────────────────┤
│ المتبقي                          │
│     $ 1,800.00                  │
└─────────────────────────────────┘
```

**قسم دينار عراقي (IQD):**

```
┌─────────────────────────────────┐
│ 💰 دينار عراقي (IQD)           │
├─────────────────────────────────┤
│ إجمالي المبيعات                 │
│     7,500,000 IQD               │
├─────────────────────────────────┤
│ إجمالي المدفوع                  │
│     4,500,000 IQD               │
├─────────────────────────────────┤
│ المتبقي                          │
│     3,000,000 IQD               │
└─────────────────────────────────┘
```

---

## 🔍 التفاصيل التقنية

### استعلام الباك إند

```javascript
// في getSalesReport()
const salesByCurrency = {
  USD: { totalSales: 5000, totalPaid: 3200, ... },
  IQD: { totalSales: 7500000, totalPaid: 4500000, ... }
};

// النتيجة المُرجعة
return {
  ...reportData,
  currencyStats: {
    USD: salesByCurrency['USD'] || { totalSales: 0, totalPaid: 0, ... },
    IQD: salesByCurrency['IQD'] || { totalSales: 0, totalPaid: 0, ... }
  },
  salesUSD: salesByCurrency['USD']?.totalSales || 0,
  paidUSD: salesByCurrency['USD']?.totalPaid || 0,
  salesIQD: salesByCurrency['IQD']?.totalSales || 0,
  paidIQD: salesByCurrency['IQD']?.totalPaid || 0
};
```

### عرض الفرونت إند

```vue
<template>
  <v-row>
    <!-- USD Section -->
    <v-col cols="12" md="6">
      <v-card>
        <h3>دولار أمريكي (USD)</h3>

        <!-- إجمالي المبيعات -->
        <div>{{ formatUSD(report.salesUSD || 0) }}</div>

        <!-- إجمالي المدفوع -->
        <div>{{ formatUSD(report.paidUSD || 0) }}</div>

        <!-- المتبقي (محسوب تلقائياً) -->
        <div>{{ formatUSD((report.salesUSD || 0) - (report.paidUSD || 0)) }}</div>
      </v-card>
    </v-col>

    <!-- IQD Section -->
    <v-col cols="12" md="6">
      <!-- نفس البنية للدينار -->
    </v-col>
  </v-row>
</template>
```

---

## 🎨 التصميم والألوان

### ألوان USD (دولار):

- **خلفية:** `blue-lighten-5`
- **المبيعات:** `green-lighten-4` (أخضر)
- **المدفوع:** `blue-lighten-4` (أزرق)
- **المتبقي:** `orange-lighten-4` (برتقالي)

### ألوان IQD (دينار):

- **خلفية:** `amber-lighten-5`
- **المبيعات:** `green-lighten-4` (أخضر)
- **المدفوع:** `blue-lighten-4` (أزرق)
- **المتبقي:** `orange-lighten-4` (برتقالي)

---

## 📝 ملاحظات هامة

### ✅ دقة البيانات

- جميع الحسابات تتم في الباك إند
- البيانات جاهزة ودقيقة
- لا حاجة لمعالجة في الفرونت إند

### ✅ التوافق

- يعمل مع جميع فلاتر التقارير
- متوافق مع التصدير (Excel/PDF)
- يظهر حتى لو لم تكن هناك مبيعات (يعرض 0)

### ✅ الأداء

- لا تأثير على الأداء
- الاستعلام نفسه يجمع البيانات
- عرض سريع وفوري

---

## 🚀 كيفية الاستخدام

### 1. فتح صفحة التقارير

```
التقارير والتحليلات → اختر التاريخ → عرض التقرير
```

### 2. عرض الإحصائيات

- انتقل للأسفل بعد البطاقات الرئيسية
- ستجد قسم "الإحصائيات حسب العملة"
- يعرض بيانات USD و IQD بشكل منفصل

### 3. التصدير

- انقر **تصدير Excel** أو **تصدير PDF**
- سيتم تضمين إحصائيات العملات تلقائياً

---

## 📚 الملفات المعدلة

### الباك إند:

```
packages/backend/src/services/saleService.js
└── getSalesReport() - إضافة currencyStats, salesUSD, paidUSD, salesIQD, paidIQD
```

### الفرونت إند:

```
packages/frontend/src/views/Reports.vue
├── template: إضافة قسم "الإحصائيات حسب العملة"
├── script: إضافة formatUSD() و formatIQD()
├── exportToExcel(): تحديث لإضافة إحصائيات العملات
└── exportToPDF(): تحديث لإضافة جدول إحصائيات العملات
```

---

## ✨ الخلاصة

تم تنفيذ جميع المتطلبات بدقة:

✅ **عرض إجمالي المدفوع بالدينار** - واضح ومنسق  
✅ **عرض إجمالي المدفوع بالدولار** - واضح ومنسق  
✅ **عرض المبيعات بالدولار** - منفصل ومباشر  
✅ **عرض المبيعات بالدينار** - منفصل ومباشر  
✅ **تحديث ديناميكي** - تلقائي مع كل استعلام  
✅ **تنسيق صحيح** - أرقام وعملات واضحة  
✅ **واجهة محسّنة** - سهلة القراءة والفهم  
✅ **أداء ممتاز** - لا تأثير على السرعة

---

**النظام الآن يعرض معلومات مالية دقيقة وواضحة تساعد في اتخاذ قرارات مدروسة!** 🎉
