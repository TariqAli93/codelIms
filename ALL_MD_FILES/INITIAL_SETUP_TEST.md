# 🧪 اختبار نظام الإعداد الأولي - Initial Setup Dialog Test

## الهدف

اختبار تكامل Backend ↔ Frontend لنظام الإعداد الأولي التلقائي.

---

## الإعداد السريع ⚡

### 1. إعادة تعيين قاعدة البيانات

```bash
cd packages/backend
rm -rf data/database.db data/.initial-setup.json
```

### 2. تشغيل Backend

```bash
cd packages/backend
pnpm dev
```

**✅ يجب أن ترى رسالة ترحيب كبيرة في Console مع بيانات الدخول**

### 3. تشغيل Frontend (في terminal جديد)

```bash
cd packages/frontend
pnpm dev
```

### 4. فتح التطبيق

افتح: http://localhost:5173

**✅ يجب أن يظهر Dialog احترافي مع:**

- اسم المستخدم: `admin`
- كلمة المرور: `Admin@123`
- أزرار نسخ للحافظة
- تحذيرات أمنية

---

## الاختبارات 🧪

### ✅ Test 1: Dialog يظهر تلقائياً

- [ ] Dialog ظهر بعد 500ms من فتح التطبيق
- [ ] العنوان: "🎉 مرحباً بك في CodeLIMS"
- [ ] رسالة: "تم إنشاء حساب المدير بنجاح!"

### ✅ Test 2: عرض البيانات

- [ ] اسم المستخدم معروض: `admin`
- [ ] كلمة المرور معروضة: `Admin@123`
- [ ] الاسم الكامل معروض: `مدير النظام`

### ✅ Test 3: أزرار النسخ

- [ ] زر نسخ username يعمل → رسالة "تم نسخ اسم المستخدم بنجاح"
- [ ] زر نسخ password يعمل → رسالة "تم نسخ كلمة المرور بنجاح"

### ✅ Test 4: التحذيرات الأمنية

- [ ] تحذير أمني ظاهر في الأسفل
- [ ] يحتوي على: "قم بتغيير كلمة المرور فوراً"
- [ ] يحتوي على: "لن تتمكن من رؤية هذه الرسالة مرة أخرى"

### ✅ Test 5: إغلاق Dialog

- [ ] زر "فهمت، دعني أبدأ" موجود
- [ ] عند الضغط، Dialog يُغلق

### ✅ Test 6: حذف الملف التلقائي

```bash
ls packages/backend/data/.initial-setup.json
# ❌ يجب أن يعطي خطأ "No such file"
```

### ✅ Test 7: لا يظهر مرة أخرى

- [ ] أغلق التطبيق وافتحه مرة أخرى
- [ ] Dialog **لا يظهر** مرة أخرى ✅

### ✅ Test 8: تسجيل الدخول

- [ ] اذهب لصفحة Login
- [ ] استخدم: `admin` / `Admin@123`
- [ ] تسجيل الدخول **ناجح** ✅

---

## الأخطاء الشائعة 🔍

### ❌ Dialog لا يظهر

**الحل**:

```bash
# تحقق من وجود الملف
ls packages/backend/data/.initial-setup.json

# إذا لم يوجد، أعد تشغيل Backend
rm packages/backend/data/database.db
cd packages/backend && pnpm dev
```

### ❌ خطأ 404 في API

**السبب**: الملف `.initial-setup.json` غير موجود (طبيعي بعد القراءة الأولى)

**الحل**: لا حاجة لفعل شيء، هذا سلوك متوقع

---

## النتيجة المتوقعة 🎉

إذا نجحت جميع الاختبارات:

✅ Backend: يُنشئ المستخدم الافتراضي + الأدوار + الصلاحيات  
✅ Backend: يحفظ البيانات في `.initial-setup.json`  
✅ Frontend: يعرض Dialog احترافي مع البيانات  
✅ Frontend: أزرار النسخ تعمل  
✅ Security: الملف يُحذف بعد القراءة (مرة واحدة فقط)  
✅ UX: تحذيرات أمنية واضحة

**النظام يعمل بشكل مثالي! 🚀**

---

**الإصدار**: 2.0.0  
**التحديث**: ديسمبر 2024
