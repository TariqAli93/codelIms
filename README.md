# CodeLIMS - نظام متكامل لإدارة الحسابات والمبيعات

<div dir="rtl">

## نظرة عامة
CodeLIMS هو نظام احترافي متكامل لإدارة الحسابات والمبيعات مبني بتقنيات حديثة:
- **Backend**: Fastify + SQLite + Drizzle ORM
- **Frontend**: Electron + Vue 3 + Vuetify 3
- **الأمان**: JWT Authentication + RBAC System
- **دعم متعدد العملات**: USD و IQD

## المميزات الرئيسية

### 📊 إدارة شاملة
- **لوحة تحكم تفاعلية** مع إحصائيات مباشرة
- **إدارة العملاء** مع متابعة المشتريات والديون
- **إدارة المنتجات** مع تتبع المخزون
- **التصنيفات** لتنظيم المنتجات

### 💰 نظام المبيعات المتقدم
- بيع نقدي وتقسيط
- دعم الخصومات والضرائب
- فواتير تفصيلية
- متابعة الدفعات والأقساط

### 💱 دعم العملات
- الدولار الأمريكي (USD)
- الدينار العراقي (IQD)
- تحويل تلقائي بين العملات
- عرض التقارير بكلا العملتين

### 🔐 الأمان والصلاحيات
- نظام مصادقة JWT
- إدارة الأدوار والصلاحيات (RBAC)
- تشفير كلمات المرور
- حماية ضد الهجمات

### 📈 التقارير والتحليلات
- تقارير المبيعات الدورية
- تحليل أداء المنتجات
- متابعة الديون والمدفوعات
- رسوم بيانية تفاعلية

## التقنيات المستخدمة

### Backend
- **Fastify** - إطار عمل سريع وفعال
- **SQLite** - قاعدة بيانات خفيفة
- **Drizzle ORM** - ORM حديث
- **Zod** - التحقق من صحة البيانات
- **JWT** - المصادقة الآمنة
- **bcryptjs** - تشفير كلمات المرور
- **Pino** - تسجيل الأحداث

### Frontend
- **Electron** - تطبيقات سطح المكتب
- **Vue 3** - إطار عمل تفاعلي حديث
- **Vuetify 3** - مكتبة مكونات Material Design
- **Vite** - أداة بناء سريعة
- **Pinia** - إدارة الحالة
- **Vue Router** - التوجيه
- **Axios** - طلبات HTTP

## متطلبات التشغيل
- Node.js >= 20.0.0
- pnpm >= 9.0.0

## التثبيت والإعداد

### 1. تثبيت المكتبات
```bash
# تثبيت pnpm إذا لم يكن مثبتًا
npm install -g pnpm

# تثبيت المكتبات لجميع الحزم
pnpm install
```

### 2. إعداد Backend

```bash
# الانتقال إلى مجلد Backend
cd packages/backend

# نسخ ملف البيئة
cp .env.example .env

# تحرير ملف .env وتعيين القيم المناسبة
# ثم إنشاء قاعدة البيانات
pnpm db:generate
pnpm db:migrate

# إضافة البيانات الأولية (مستخدم admin)
node src/seed.js
```

**بيانات تسجيل الدخول الافتراضية:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

### 3. تشغيل التطبيق

#### وضع التطوير
```bash
# في نافذة Terminal أولى - تشغيل Backend
pnpm dev:backend

# في نافذة Terminal ثانية - تشغيل Frontend
pnpm dev
```

#### بناء تطبيق Electron
```bash
# بناء التطبيق للإنتاج
pnpm build:electron
```

الملف التنفيذي سيكون في: `packages/frontend/dist-electron/`

## هيكل المشروع

```
codelIms/
├── packages/
│   ├── backend/                 # خادم Fastify API
│   │   ├── src/
│   │   │   ├── config.js       # إعدادات التطبيق
│   │   │   ├── server.js       # نقطة البداية
│   │   │   ├── db.js           # اتصال قاعدة البيانات
│   │   │   ├── seed.js         # البيانات الأولية
│   │   │   ├── models/         # Drizzle Schema
│   │   │   ├── controllers/    # معالجات الطلبات
│   │   │   ├── services/       # منطق العمل
│   │   │   ├── routes/         # مسارات API
│   │   │   ├── plugins/        # إضافات Fastify
│   │   │   └── utils/          # أدوات مساعدة
│   │   └── package.json
│   │
│   └── frontend/               # تطبيق Electron + Vue
│       ├── electron/           # ملفات Electron
│       ├── src/
│       │   ├── main.js        # نقطة البداية
│       │   ├── App.vue        # المكون الرئيسي
│       │   ├── router/        # التوجيه
│       │   ├── stores/        # Pinia Stores
│       │   ├── views/         # صفحات التطبيق
│       │   ├── layouts/       # القوالب
│       │   ├── plugins/       # الإضافات
│       │   └── styles/        # الأنماط
│       └── package.json
│
├── pnpm-workspace.yaml        # إعدادات Monorepo
├── package.json               # الحزمة الجذر
└── README.md                  # هذا الملف
```

## API Endpoints

### Authentication
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `GET /api/auth/profile` - الحصول على الملف الشخصي

### Customers
- `GET /api/customers` - قائمة العملاء
- `POST /api/customers` - إضافة عميل
- `GET /api/customers/:id` - تفاصيل عميل
- `PUT /api/customers/:id` - تحديث عميل
- `DELETE /api/customers/:id` - حذف عميل

### Products
- `GET /api/products` - قائمة المنتجات
- `POST /api/products` - إضافة منتج
- `GET /api/products/:id` - تفاصيل منتج
- `PUT /api/products/:id` - تحديث منتج
- `DELETE /api/products/:id` - حذف منتج
- `GET /api/products/low-stock` - منتجات قليلة المخزون

### Categories
- `GET /api/categories` - قائمة التصنيفات
- `POST /api/categories` - إضافة تصنيف
- `GET /api/categories/:id` - تفاصيل تصنيف
- `PUT /api/categories/:id` - تحديث تصنيف
- `DELETE /api/categories/:id` - حذف تصنيف

### Sales
- `GET /api/sales` - قائمة المبيعات
- `POST /api/sales` - إنشاء بيع
- `GET /api/sales/:id` - تفاصيل بيع
- `POST /api/sales/:id/cancel` - إلغاء بيع
- `GET /api/sales/report` - تقرير المبيعات

## نظام الصلاحيات

### الأدوار الافتراضية
1. **Admin** - كامل الصلاحيات
2. **Manager** - صلاحيات محدودة
3. **Sales** - صلاحيات المبيعات فقط

### الصلاحيات
- `customers:create` - إنشاء عملاء
- `customers:read` - عرض العملاء
- `customers:update` - تحديث العملاء
- `customers:delete` - حذف العملاء
- `products:*` - صلاحيات المنتجات
- `sales:*` - صلاحيات المبيعات
- `categories:*` - صلاحيات التصنيفات

## أوامر مفيدة

```bash
# تشغيل Backend في وضع التطوير
pnpm dev:backend

# تشغيل Frontend في وضع التطوير
pnpm dev

# تشغيل Electron في وضع التطوير
cd packages/frontend
pnpm electron:dev

# بناء للإنتاج
pnpm build

# بناء تطبيق Electron
pnpm build:electron

# فحص الكود
pnpm lint

# تنسيق الكود
pnpm format

# إدارة قاعدة البيانات
cd packages/backend
pnpm db:generate    # إنشاء ملفات Migration
pnpm db:migrate     # تطبيق Migrations
pnpm db:studio      # فتح Drizzle Studio
```

## الأمان

- جميع كلمات المرور مشفرة باستخدام bcrypt
- المصادقة عبر JWT tokens
- حماية ضد CSRF و XSS
- Rate limiting للحماية من هجمات DDoS
- Helmet لإضافة HTTP security headers
- التحقق من صحة جميع المدخلات باستخدام Zod

## التطوير المستقبلي

- [ ] تكامل مع أنظمة الدفع الإلكتروني
- [ ] تطبيق موبايل (React Native)
- [ ] تقارير متقدمة مع PDF export
- [ ] نظام إشعارات
- [ ] دعم عملات إضافية
- [ ] نسخ احتياطي تلقائي
- [ ] تزامن عبر السحابة

## الدعم

للحصول على الدعم أو الإبلاغ عن مشاكل:
- البريد الإلكتروني: support@codelims.com
- المستندات: [docs.codelims.com](https://docs.codelims.com)

## الترخيص
MIT License

## المطورون
CodeLIMS Team © 2025

---

**ملاحظة:** تأكد من تغيير كلمة المرور الافتراضية وإعدادات JWT_SECRET في بيئة الإنتاج!

</div>
